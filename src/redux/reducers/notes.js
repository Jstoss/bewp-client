const initialState = {
  notes: [],
  fetchingNotes: false,
  showingModal: false,
  error: null
};

export const noteReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
