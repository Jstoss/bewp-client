const initialState = {
  notes: [],
  fetchingNotes: false,
  showingModal: false,
  error: null
};

export const note = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
