const initialState = {
  isLoggedIn: false,
  username: null,
  token: null
};

export const userReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
