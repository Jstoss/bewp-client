const initialState = {
  isLoggedIn: false,
  username: null,
  token: null
};

export const user = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
